<?xml version="1.0" encoding="UTF-8"?>
<local:DabBaseContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DABApp.DabTabletPage"
	xmlns:local="clr-namespace:DABApp;" xmlns:controls="clr-namespace:SegmentedControl.FormsPlugin.Abstractions;assembly=SegmentedControl.FormsPlugin.Abstractions"
	xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
	xmlns:ios="clr-namespace:MediaPlayer;assembly=Xamarin.iOS;targetPlatform=iOS">
	<local:DabBaseContentPage.Resources>
		<ResourceDictionary>
			<local:StringConverter x:Key="stringer"/>
			<local:InverseConverter x:Key="inverser"/>
		</ResourceDictionary>
	</local:DabBaseContentPage.Resources>
	<ContentPage.Content>
		<Grid RowSpacing="0" ColumnSpacing="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="2*"/>
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="3*"/>
			</Grid.ColumnDefinitions>
			<StackLayout Grid.Row="0" Grid.ColumnSpan="2">
				<RelativeLayout>
					<ff:CachedImage x:Name="BackgroundImage" Aspect="AspectFill" RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=1}" RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor=1}"/>
					<StackLayout Padding="20" RelativeLayout.WidthConstraint="{ConstraintExpression Type=RelativeToParent, Property=Width, Factor=1}" RelativeLayout.HeightConstraint="{ConstraintExpression Type=RelativeToParent, Property=Height, Factor=1}">
					<StackLayout x:Name="PlayerLabels" VerticalOptions="CenterAndExpand">
						<Label Text="{Binding title}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="30" Style="{StaticResource playerLabelStyle}"/>
						<Label Text="{Binding channel_title}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Micro" TextColor="Gray"/>
						<Label Text="{Binding description}" HorizontalOptions="Center" HorizontalTextAlignment="Center" FontSize="Medium" Style="{StaticResource playerLabelStyle}"/>
					</StackLayout>
			<Grid VerticalOptions="StartAndExpand" x:Name="PlayerControls">
						<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="17*"/>
								<ColumnDefinition Width="17*"/>
								<ColumnDefinition Width="32*"/>
								<ColumnDefinition Width="17*"/>
								<ColumnDefinition Width="17*"/>
							</Grid.ColumnDefinitions>
				<local:AudioOutputView x:Name="Output" WidthRequest="35" Grid.Row="0" Grid.Column="0"/>
				<Button x:Name="backwardButton" Clicked="OnBack30" Style="{StaticResource imageButtonStyle}" Image="ic_replay_30_white.png" Grid.Row="0" Grid.Column="1"/>
				<Button x:Name="PlayPause" Image="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=PlayPauseButtonImageBig}" Clicked="OnPlay" Style="{StaticResource imageButtonStyle}" Grid.Row="0" Grid.Column="2"/>
				<Button x:Name="Initializer" Clicked="OnInitialized" IsVisible="false" Image="ic_play_circle_outline_white_2x.png" Style="{StaticResource imageButtonStyle}" Grid.Row="0" Grid.Column="2"/>
				<Button x:Name="forwardButton" Clicked="OnForward30" Style="{StaticResource imageButtonStyle}" Image="ic_forward_30_white.png" Grid.Row="0" Grid.Column="3"/>
				<Button x:Name="Share" Clicked="OnShare" Image="ic_share_white_2x.png" Style="{StaticResource imageButtonStyle}" Grid.Row="0" Grid.Column="4"/>
			</Grid>
			<local:DabSeekBar x:Name="SeekBar" Minimum="0" VerticalOptions="EndAndExpand" UserInteraction="OnTouched"
					Value="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=CurrentTime}"
					Maximum="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=TotalTime}"/>
			<StackLayout Orientation="Horizontal" VerticalOptions="StartAndExpand" x:Name="TimeStrings">
				<Label Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=CurrentTime, Converter={StaticResource stringer}}" HorizontalOptions="CenterAndExpand" Style="{StaticResource playerLabelStyle}" FontSize="Small"/>
				<Label Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=RemainingTime}" HorizontalOptions="CenterAndExpand" Style="{StaticResource playerLabelStyle}" FontSize="Small"/>
			</StackLayout>
					</StackLayout>
				</RelativeLayout>
			</StackLayout>
			<StackLayout Grid.Row="1" Grid.Column="0" Padding="10" BackgroundColor="{DynamicResource InputBackgroundColor}">
				<Label FontSize="Small" Text="Channels" Style="{StaticResource secondaryLabelStyle}" FontAttributes="Bold"/>
				<ListView x:Name="ChannelsList" BackgroundColor="Transparent" ItemSelected="OnChannel">
					<ListView.ItemTemplate>
						<DataTemplate>
							<ViewCell>
								<StackLayout Padding="10">
									<Label Text="{Binding title}" FontSize="Medium" VerticalOptions="Center"/>
								</StackLayout>
							</ViewCell>
						</DataTemplate>
					</ListView.ItemTemplate>
				</ListView>
			</StackLayout>
			<StackLayout Grid.Row="1" Grid.Column="1">
				<StackLayout Padding="20" Spacing="10">
					<controls:SegmentedControl x:Name="SegControl" TintColor="White" SelectedSegment="0" ValueChanged="Handle_ValueChanged">
						<controls:SegmentedControl.Children>
							<controls:SegmentedControlOption Text="Archive"/>
							<controls:SegmentedControlOption Text="Read"/>
							<controls:SegmentedControlOption Text="Journal"/>
						</controls:SegmentedControl.Children>
					</controls:SegmentedControl>
					<BoxView x:Name="Divider" HeightRequest="1" HorizontalOptions="FillAndExpand" Color="{DynamicResource InputBackgroundColor}"/>
					<!--<Frame Style="{StaticResource secondaryFrameStyle}"/>-->
				</StackLayout>
				<ScrollView x:Name="Archive" IsVisible="true">
				<StackLayout Padding="20" Spacing="20">
			<StackLayout Orientation="Horizontal">
				<Picker x:Name="Months" SelectedIndexChanged="OnMonthSelected" HorizontalOptions="Start" WidthRequest="100" BackgroundColor="Transparent" TextColor="White"  />
				<Label Text="Make available offline" HorizontalOptions="StartAndExpand" VerticalOptions="Center" FontSize="Medium"  Style="{StaticResource playerLabelStyle}"/>
				<Switch x:Name="Offline" HorizontalOptions="End" VerticalOptions="Center" Toggled="OnOffline"/>
			</StackLayout>
						<BoxView HeightRequest="1" HorizontalOptions="FillAndExpand" Color="{DynamicResource InputBackgroundColor}"/>
						<!--<Frame Style="{StaticResource secondaryFrameStyle}"/>-->
			<local:NonScrollingListView x:Name="EpisodeList" BackgroundColor="Transparent" HasUnevenRows="true" ItemTapped="OnEpisode">
				<local:NonScrollingListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<Grid Padding="10,10,10,10">
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="*"/>
								</Grid.RowDefinitions>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*"/>
									<ColumnDefinition Width="Auto"/>
								</Grid.ColumnDefinitions>
								<StackLayout Orientation="Horizontal" Grid.Row="0" Grid.Column="0">
									<Label Text="{Binding title}" FontSize="Medium" HorizontalOptions="Start"/>
									<!--<Label Text="{Binding PubMonth}" FontSize="Medium" HorizontalOptions="Start"/>
									<Label Text="{Binding PubDay}" FontSize="Large"/>-->
							<Image IsVisible="{Binding downloadVisible}"  Source="ic_cloud_download_white"  Opacity=".5" HeightRequest="15" Aspect="AspectFit" />
								</StackLayout>
								<Label Text="{Binding description}" FontSize="Micro" Grid.Row="1" Grid.Column="0" Style="{StaticResource secondaryLabelStyle}" LineBreakMode="TailTruncation"/>
								<local:ImageCircle BackgroundColor="{DynamicResource HighlightColor}" IsVisible="{Binding listenedToVisible}" Grid.RowSpan="2" Grid.Column="1" WidthRequest="10" HeightRequest="10" VerticalOptions="Center"/>
							</Grid>
						</ViewCell>
					</DataTemplate>
				</local:NonScrollingListView.ItemTemplate>
			</local:NonScrollingListView>
			</StackLayout>
			</ScrollView>
				<StackLayout x:Name="Read" IsVisible="false" Padding="20" VerticalOptions="StartAndExpand">
					<Label x:Name="ReadTitle" VerticalOptions="Start" FontSize="Large"/>
					<Label x:Name="ReadExcerpts" VerticalOptions="Start" FontSize="Medium"/>
					<Label x:Name="AltWarning" IsVisible="false" VerticalOptions="Start" FontSize="Micro" Text="Due to copyright restrictions, the passage displayed here may be different than the passage being read."/>
					<ScrollView>
					<local:HtmlLabel x:Name="ReadText" VerticalOptions="StartAndExpand"/>
					</ScrollView>
			</StackLayout>
				<StackLayout x:Name="LoginJournal" IsVisible="false" VerticalOptions="StartAndExpand" Padding="20" Spacing="20">
					<Label Text="You need to be logged in to use this feature." FontSize="Large" FontAttributes="Bold"/>
					<Label Text="Create an account and join us each day as we read through the bible every year." FontSize="Medium"/>
					<Button x:Name="Login" Text="Log in" Clicked="OnLogin" Style="{StaticResource highlightedButtonStyle}"/>
				</StackLayout>
				<StackLayout x:Name="Journal" IsVisible="false" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Padding="20">
					<Label x:Name="JournalTitle" Text="{Binding title}" FontSize="Micro"/>
					<local:DarkKeyboardEditor x:Name="JournalContent" Text="{Binding Source={x:Static local:JournalTracker.Current}, Path=Content}" HeightRequest="500" TextChanged="OnJournalChanged" Focused="OnEdit" Unfocused="OffEdit"/>
					<Label x:Name="JournalWarning" Text="You must be connected to the journal server inorder for changes to be saved." Style="{StaticResource warningLabelStyle}" IsVisible="{Binding Source={x:Static local:JournalTracker.Current}, Path=IsConnected, Converter={StaticResource inverser}}"/>
					<BoxView x:Name="spacer" BackgroundColor="Transparent"/>
				</StackLayout>
			</StackLayout>
		</Grid>
	</ContentPage.Content>
</local:DabBaseContentPage>
