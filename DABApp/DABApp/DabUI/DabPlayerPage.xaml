<?xml version="1.0" encoding="UTF-8"?>
<local:DabBaseContentPage xmlns:t="clr-namespace:SlideOverKit;assembly=SlideOverKit" xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="DABApp.DabPlayerPage" xmlns:local="clr-namespace:DABApp;assembly=DABApp"
		xmlns:ios="clr-namespace:MediaPlayer;assembly=Xamarin.iOS;targetPlatform=iOS" 
		xmlns:controls="clr-namespace:SegmentedControl.FormsPlugin.Abstractions;assembly=SegmentedControl.FormsPlugin.Abstractions">
	<t:MenuContainerPage.Content>
		<StackLayout x:Name="Container" VerticalOptions="FillAndExpand">
			<StackLayout Padding="20" Spacing="20">
			<controls:SegmentedControl x:Name="SegControl" TintColor="White" SelectedSegment="0" ValueChanged="Handle_ValueChanged">
				<controls:SegmentedControl.Children>
					<controls:SegmentedControlOption Text="Listen"/>
					<controls:SegmentedControlOption Text="Read"/>
					<controls:SegmentedControlOption Text="Journal"/>
				</controls:SegmentedControl.Children>
			</controls:SegmentedControl>
				</StackLayout>
			<StackLayout x:Name="Listen" IsVisible="true"  VerticalOptions="FillAndExpand" Padding="20" Spacing="20">
			<Label Text="Player"/>
			<Slider x:Name="SeekBar" HorizontalOptions="FillAndExpand" Minimum="0"  
					Value="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=CurrentTime}"
					Maximum="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=TotalTime}"/>
			<StackLayout Orientation="Horizontal">
				<Label Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=CurrentTime}" HorizontalOptions="StartAndExpand"/>
				<Label Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=RemainingTime}" HorizontalOptions="EndAndExpand"/>
			</StackLayout>
			<StackLayout Orientation="Horizontal">
				<Button Text="Back 30" Clicked="OnBack30" HorizontalOptions="StartAndExpand"/>
				<Button Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=PlayButtonText}" Clicked="OnPlay" HorizontalOptions="CenterAndExpand"/>
				<Button Text="Forward 30" Clicked="OnForward30" HorizontalOptions="EndAndExpand"/>
			</StackLayout>
				</StackLayout>
			<ScrollView x:Name="Read" IsVisible="false" VerticalOptions="FillAndExpand"  HeightRequest="1200">
			<StackLayout VerticalOptions="FillAndExpand" Padding="20" Spacing="20">
				<local:HtmlLabel x:Name="HLabel" />
			</StackLayout>
				</ScrollView>
			<StackLayout x:Name="Journal" IsVisible="false"  VerticalOptions="FillAndExpand" Padding="20" Spacing="20">
				<Label Text="Date" />
				<Entry Placeholder="Your text here."/>
			</StackLayout>
			<StackLayout x:Name="stackFooterContent" VerticalOptions="End" HeightRequest="70" IsVisible="false">
				<StackLayout Orientation="Horizontal" VerticalOptions="End" HeightRequest="40">
					<Button x:Name="PlayPause" Text="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=PlayButtonText}" HorizontalOptions="Start" WidthRequest="75" Clicked="OnPlayPause"/>
					<Button x:Name="PodcastTitle" Text="Podcast Title" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" Clicked="OnPodcast"/>
				</StackLayout>
					<ProgressBar Progress="{Binding Source={x:Static local:AudioPlayer.Instance}, Path=Progress}" VerticalOptions="End" HeightRequest="30"/>
			</StackLayout>
		</StackLayout>
	</t:MenuContainerPage.Content>
</local:DabBaseContentPage>